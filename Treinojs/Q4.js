const prompt = require('prompt-sync')();
let nome = prompt('Conta-me teu nome: ')
let idade = Number(prompt('E quantos anos você tem?'))
let disney = prompt('Você já visitou a disney (true/false)')
disney = (disney.toLowerCase() === 'true')

let dados = {
    
}